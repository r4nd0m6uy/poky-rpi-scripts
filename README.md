# Yocto Raspberry PI build scripts
A set of scripts to automatically build an image for the raspberry PI.

# Usage
To build an image, just run the provided script:
> ./build.sh

# TODO
This is work in progress ...:

* Let the user edit its local.conf
* Add more switch to the build.sh script (checkout, init, build, ...)

